import { AlertCircle, CheckCircle2, Clock3 } from 'lucide-react'
import { PageHeader } from '../../../components/common/PageHeader'
import { StatusBadge } from '../../../components/common/StatusBadge'
import { LoadingSkeleton } from '../../../components/ui/LoadingSkeleton'
import { useOptimizationRequests } from '../hooks/useProductionDashboard'
import { formatProductionDate } from '../utils/productionFormatters'
export function OptimizationRequestsPage(){const query=useOptimizationRequests();if(query.isLoading)return <LoadingSkeleton lines={6}/>;return <div className="space-y-6"><PageHeader eyebrow="Workflow Commercial" title="Demandes d’optimisation" description="Retours structurés du Responsable Commercial sur les produits étudiés."/><div className="grid gap-4">{query.data?.map(r=><article key={r.id} className="surface-card p-5"><div className="flex flex-col justify-between gap-3 sm:flex-row"><div><div className="flex flex-wrap items-center gap-2"><h2 className="font-bold text-slate-950">{r.productName} · v{r.productVersion}</h2><StatusBadge label={r.priority} tone={r.priority==='Urgent'?'danger':r.priority==='High'?'warning':'info'}/><StatusBadge label={r.status} tone={r.status==='Resolved'?'success':'neutral'}/></div><p className="mt-2 text-sm text-slate-600">{r.message}</p><p className="mt-2 text-xs text-slate-400">{r.commercialManagerName} · {formatProductionDate(r.createdAt)}</p></div><span className="grid size-11 shrink-0 place-items-center rounded-xl bg-amber-50 text-amber-600">{r.status==='Resolved'?<CheckCircle2/>:r.status==='InProgress'?<Clock3/>:<AlertCircle/>}</span></div><ul className="mt-4 grid gap-2 sm:grid-cols-2">{r.requestedChanges.map(c=><li key={c} className="rounded-lg bg-slate-50 px-3 py-2 text-sm text-slate-700">• {c}</li>)}</ul></article>)}</div></div>}
