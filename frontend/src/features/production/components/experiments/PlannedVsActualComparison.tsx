import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import type { ProductionExperiment } from '../../types/production.types'
export function PlannedVsActualComparison({experiment}:{experiment:ProductionExperiment}){const data=[{name:'Coût (MAD)',prévu:experiment.plannedCost,réel:experiment.actualCost},{name:'Durée (min)',prévu:experiment.plannedDuration,réel:experiment.actualDuration},{name:'Quantité',prévu:experiment.plannedQuantity,réel:experiment.actualQuantity}];return <div className="h-72"><ResponsiveContainer width="100%" height="100%"><BarChart data={data}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="name"/><YAxis/><Tooltip/><Bar dataKey="prévu" fill="#94a3b8" radius={[6,6,0,0]}/><Bar dataKey="réel" fill="#2563eb" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div>}
