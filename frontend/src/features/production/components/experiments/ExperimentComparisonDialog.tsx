import { Modal } from '../../../../components/ui/Modal'
import type { ProductionExperiment } from '../../types/production.types'
import { formatCurrency } from '../../utils/productionFormatters'
import { ExperimentResultBadge } from './ExperimentResultBadge'
export function ExperimentComparisonDialog({open,experiments,onClose}:{open:boolean;experiments:ProductionExperiment[];onClose:()=>void}){return <Modal open={open} onClose={onClose} title="Comparer deux expériences" className="max-w-4xl"><div className="grid gap-4 md:grid-cols-2">{experiments.slice(0,2).map(e=><article key={e.id} className="rounded-xl border border-slate-200 p-4"><div className="flex justify-between gap-2"><h3 className="font-bold">{e.name}</h3><ExperimentResultBadge result={e.result}/></div><dl className="mt-4 grid grid-cols-2 gap-3 text-sm"><div><dt className="text-slate-400">Coût</dt><dd>{formatCurrency(e.actualCost)}</dd></div><div><dt className="text-slate-400">Durée</dt><dd>{e.actualDuration} min</dd></div><div><dt className="text-slate-400">Quantité</dt><dd>{e.actualQuantity}</dd></div><div><dt className="text-slate-400">Pertes</dt><dd>{e.wasteRate}%</dd></div></dl></article>)}</div></Modal>}
