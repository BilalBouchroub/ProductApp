import { ArrowRight, Clock, Coins, Recycle } from 'lucide-react'
import { Link } from 'react-router-dom'
import type { ProductionExperiment } from '../../types/production.types'
import { getMainVariance } from '../../utils/experimentCalculations'
import { formatCurrency, formatProductionDate } from '../../utils/productionFormatters'
import { ExperimentResultBadge } from './ExperimentResultBadge'
export function ExperimentCard({experiment}:{experiment:ProductionExperiment}){return <article className="surface-card p-5"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-wider text-blue-600">{experiment.productName} · v{experiment.productVersion}</p><h3 className="mt-1 font-bold text-slate-950">{experiment.name}</h3><p className="mt-1 text-xs text-slate-400">{formatProductionDate(experiment.startDate)}</p></div><ExperimentResultBadge result={experiment.result}/></div><div className="mt-5 grid grid-cols-3 gap-2"><Metric icon={Coins} label="Coût" value={`${formatCurrency(experiment.plannedCost)} → ${formatCurrency(experiment.actualCost)}`}/><Metric icon={Clock} label="Durée" value={`${experiment.plannedDuration} → ${experiment.actualDuration} min`}/><Metric icon={Recycle} label="Perte" value={`${experiment.wasteRate}%`}/></div><div className="mt-4 rounded-xl bg-amber-50 p-3 text-xs text-amber-800"><strong>Écart principal :</strong> {getMainVariance(experiment)}</div><Link to={`/production/experiments/${experiment.id}`} className="mt-4 inline-flex items-center gap-1 text-sm font-semibold text-blue-600">Consulter <ArrowRight className="size-4"/></Link></article>}
function Metric({icon:Icon,label,value}:{icon:typeof Coins;label:string;value:string}){return <div className="rounded-lg bg-slate-50 p-2"><Icon className="size-3.5 text-slate-400"/><p className="mt-1 text-[10px] uppercase text-slate-400">{label}</p><p className="truncate text-xs font-semibold text-slate-700" title={value}>{value}</p></div>}
