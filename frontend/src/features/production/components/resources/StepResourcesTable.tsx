import { Pencil, Trash2 } from 'lucide-react'
import { Button } from '../../../../components/ui/Button'
import type { StepResource } from '../../types/production.types'
import { formatCurrency, formatQuantity } from '../../utils/productionFormatters'
import { ResourceAvailabilityBadge } from './ResourceAvailabilityBadge'
export function StepResourcesTable({resources,onEdit,onDelete}:{resources:StepResource[];onEdit:(r:StepResource)=>void;onDelete:(r:StepResource)=>void}){if(!resources.length)return <div className="rounded-xl border border-dashed border-slate-300 p-6 text-center text-sm text-slate-500">Aucune ressource associée à cette étape.</div>;return <div className="overflow-x-auto rounded-xl border border-slate-200"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="p-3">Ressource</th><th className="p-3">Quantité</th><th className="p-3">Coût</th><th className="p-3">Disponibilité</th><th className="p-3">Actions</th></tr></thead><tbody className="divide-y divide-slate-100">{resources.map((r)=><tr key={r.id}><td className="p-3"><strong className="block text-slate-800">{r.designation}</strong><span className="text-xs text-slate-400">{r.sapCode}</span></td><td className="p-3">{formatQuantity(r.plannedQuantity,r.unit)}</td><td className="p-3">{formatCurrency(r.totalCost)}</td><td className="p-3"><ResourceAvailabilityBadge status={r.availabilityStatus}/></td><td className="p-3"><Button size="icon" variant="ghost" onClick={()=>onEdit(r)} aria-label="Modifier"><Pencil className="size-4"/></Button><Button size="icon" variant="ghost" onClick={()=>onDelete(r)} aria-label="Supprimer"><Trash2 className="size-4 text-red-500"/></Button></td></tr>)}</tbody></table></div>}
