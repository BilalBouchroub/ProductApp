import { Clock3, Coins, PackageOpen, Recycle } from 'lucide-react'
import type { ProcessSummary } from '../../types/production.types'
import { formatCurrency } from '../../utils/productionFormatters'
import { minutesToLabel } from '../../utils/productionCalculations'
export function ProductionSummaryPanel({summary}:{summary:ProcessSummary}){const items=[{label:'Coût total',value:formatCurrency(summary.plannedCost),icon:Coins},{label:'Durée totale',value:minutesToLabel(summary.plannedDurationMinutes),icon:Clock3},{label:'Ressources',value:String(summary.resourceCount),icon:PackageOpen},{label:'Pertes prévues',value:`${summary.wasteQuantity.toFixed(1)} kg`,icon:Recycle}];return <aside className="surface-card p-5"><h2 className="font-bold text-slate-900">Résumé permanent</h2><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-1">{items.map(({label,value,icon:Icon})=><div key={label} className="flex items-center gap-3 rounded-xl bg-slate-50 p-3"><span className="rounded-lg bg-white p-2 text-blue-600 shadow-sm"><Icon className="size-4"/></span><div><p className="text-xs text-slate-500">{label}</p><p className="font-bold text-slate-900">{value}</p></div></div>)}</div></aside>}
