import { Check } from 'lucide-react'
const labels=['Informations','Marché','Clients','Concurrents','Rentabilité','Prévisions','Risques','Scores','Validation']
export function MarketStudyStepper({current,onSelect}:{current:number;onSelect:(step:number)=>void}){return <nav aria-label="Étapes de l’étude" className="overflow-x-auto"><ol className="flex min-w-max gap-2">{labels.map((label,index)=><li key={label}><button type="button" onClick={()=>index<=current&&onSelect(index)} disabled={index>current} aria-current={index===current?'step':undefined} className={`flex items-center gap-2 rounded-xl px-3 py-2 text-xs font-semibold transition ${index===current?'bg-blue-600 text-white':index<current?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-400'}`}><span className="grid size-5 place-items-center rounded-full bg-white/20">{index<current?<Check className="size-3"/>:index+1}</span>{label}</button></li>)}</ol></nav>}
