import { Bell, CheckCheck } from 'lucide-react'
import { useState } from 'react'
import { useAuth } from '../../hooks/useAuth'
import { useSharedNotifications } from '../../hooks/useSharedNotifications'
export function NotificationBell(){const[open,setOpen]=useState(false),{session}=useAuth();const data=useSharedNotifications(session?.user.role??'Administrator');if(!session)return null;return <div className="relative"><button type="button" onClick={()=>setOpen(v=>!v)} className="relative grid size-10 place-items-center rounded-xl text-slate-500 hover:bg-slate-100 hover:text-slate-800" aria-label={`${data.unread} notification(s) non lue(s)`} aria-expanded={open}><Bell className="size-5"/>{data.unread>0&&<span className="absolute top-2 right-2 size-2 rounded-full bg-red-500 ring-2 ring-white"/>}</button>{open&&<div className="absolute top-12 right-0 z-30 w-[min(22rem,calc(100vw-2rem))] overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-xl"><div className="flex items-center justify-between border-b border-slate-100 px-4 py-3"><div><p className="font-semibold text-slate-900">Notifications</p><p className="text-xs text-slate-500">{data.unread} nouvelle(s)</p></div><button type="button" onClick={data.markAllRead} className="rounded-lg p-2 text-blue-600 hover:bg-blue-50" aria-label="Tout marquer comme lu"><CheckCheck className="size-4"/></button></div><div className="max-h-96 divide-y divide-slate-100 overflow-y-auto">{data.notifications.length?data.notifications.map(item=><article key={item.id} className="px-4 py-3 hover:bg-slate-50"><div className="flex gap-3"><span className={`mt-1 size-2 shrink-0 rounded-full ${item.isRead?'bg-slate-300':item.type==='error'?'bg-red-500':item.type==='warning'?'bg-amber-500':'bg-blue-500'}`}/><div><p className="text-sm font-medium text-slate-800">{item.title}</p><p className="mt-1 text-xs leading-4 text-slate-500">{item.message}</p><time className="mt-1 block text-[10px] text-slate-400">{new Date(item.createdAt).toLocaleString('fr-FR')}</time></div></div></article>):<p className="p-5 text-center text-sm text-slate-500">Aucune notification.</p>}</div></div>}</div>}
