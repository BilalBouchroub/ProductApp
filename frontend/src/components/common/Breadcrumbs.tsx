import { ChevronRight, Home } from 'lucide-react'
import { Link, useLocation } from 'react-router-dom'
const labels:Record<string,string>={admin:'Administration',production:'Production',commercial:'Commercial',dashboard:'Vue d’ensemble',users:'Utilisateurs',products:'Produits',experiments:'Expériences',recent:'Récentes',new:'Nouveau',edit:'Modifier',process:'Chaîne de production','optimization-requests':'Demandes d’optimisation',studies:'Études de marché','market-study':'Nouvelle étude',comparison:'Comparaison',reports:'Rapports',result:'Résultat',roles:'Rôles et accès',logs:'Logs'}
export function Breadcrumbs(){const{pathname}=useLocation();const segments=pathname.split('/').filter(Boolean);return <nav aria-label="Fil d’Ariane" className="hidden items-center gap-1 text-sm text-slate-500 sm:flex"><Link to="/" className="rounded-md p-1 hover:bg-slate-100" aria-label="Accueil"><Home className="size-4"/></Link>{segments.map((segment,index)=>{const path=`/${segments.slice(0,index+1).join('/')}`,last=index===segments.length-1;return <span key={path} className="flex items-center gap-1"><ChevronRight className="size-3.5 text-slate-300"/>{last?<span className="max-w-40 truncate font-medium text-slate-700" aria-current="page">{labels[segment]??segment}</span>:<Link to={path} className="max-w-32 truncate hover:text-slate-800">{labels[segment]??segment}</Link>}</span>})}</nav>}
